services:
  app:
    image: zyphrzero/chatgpt2api:latest
    pull_policy: always
    container_name: chatgpt2api
    restart: unless-stopped
    ports:
      - "3000:80"
    env_file:
      - ${CHATGPT2API_ENV_FILE:-../.env}
    volumes:
      - ${CHATGPT2API_DATA_DIR:-../data}:/app/data
